<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:supportsRtl="true">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        <!-- Custom scheme — always works, no domain needed. Keep this even
             once the App Link below is verified: it's what local dev/testing
             should keep using (`adb shell am start -a android.intent.action.VIEW
             -d "lifefull://verify-email?token=..."`), and a legitimate
             fallback afterward too. -->
        <intent-filter android:autoVerify="false">
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.DEFAULT" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="lifefull" />
        </intent-filter>
        <!-- App Link — opens the app directly instead of a chooser, but ONLY
             once android:host below has /.well-known/assetlinks.json hosted
             on it (over HTTPS, this exact app's package name + signing cert
             SHA-256) and Android has verified the association (checked at
             install time). Until then this filter is harmless but inert:
             Android just can't verify it, so links to this host fall back to
             a normal browser/chooser. Paths are bare root paths — /verify and
             /reset-password, NOT /v1/auth/... — confirmed against the
             backend's actual mailer.service.ts + main.ts (2026-09-05): both
             are deliberately excluded from the API's /v1 prefix specifically
             so the app can claim them as App Link paths. /verify has a real
             GET page behind it (VerifyController) so that one genuinely
             double-duties as a browser fallback too; /reset-password does
             NOT yet (mailer.service.ts's own comment: the app "eventually
             claims" it — no browser-facing controller exists for it yet, so
             tapping this link without the app installed currently 404s).
             Pointed at the dev API domain for now (matches
             config/apiConfig.ts's API_BASE_URL) rather than waiting on the
             client's domain (docs/CLIENT_REQUIREMENTS.md #5) — swap
             android:host + config/deepLinkConfig.ts's APP_LINK_HOST together
             when that domain exists; they must match exactly. -->
        <intent-filter android:autoVerify="true">
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.DEFAULT" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="https"
                  android:host="lifefull-apis-dev.scaleupdevops.in"
                  android:pathPrefix="/verify" />
            <data android:scheme="https"
                  android:host="lifefull-apis-dev.scaleupdevops.in"
                  android:pathPrefix="/reset-password" />
        </intent-filter>
      </activity>
    </application>
</manifest>
